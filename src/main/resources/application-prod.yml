spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movieclub
    username: root
    password: 1234
  liquibase:
    contexts: prod
  web:
    resources:
      static-locations: classpath:/static/,file:./uploads/
app:
  storage:
    location: ./uploads/